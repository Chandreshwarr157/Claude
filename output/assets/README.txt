DROP YOUR CHARACTER ARTWORK HERE
================================

File name expected:  raven.png   (this exact name)

- Use a Raven X-Suit cutout with a TRANSPARENT background (PNG).
- Recommended height: 1000px+ so it stays crisp on the 1280x720 canvas.
- Pose tip (matching the references): a dynamic / action or hero stance
  reads best — full body or 3/4 body, facing slightly inward toward the text.

Once raven.png is in this folder, BOTH thumbnails load it automatically
(main character + the faded ghost duplicate behind). The dashed
placeholder box disappears on its own.

Want to point at a different file/location instead? Open the thumbnail
HTML and change this line near the top:

    --char-img: url('assets/raven.png');
